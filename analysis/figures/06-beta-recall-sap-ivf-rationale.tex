\sap+IVF's $\beta$ sweep covers a smaller range than the flat \sap
sweep because IVF probe interaction makes recall \emph{more}
sensitive to perturbation: clusters' centroids drift with $\beta$,
which compounds the within-cluster ranking error. The plaintext
ceiling line is the same upper bound as in figure 05 (full IVF
probe at $\beta = 0$). Cross-read with figure 10 (recall vs
nprobe) to disentangle the two axes (probe aggression vs noise
level).
